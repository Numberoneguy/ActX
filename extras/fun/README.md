### ActX fun .JSON files<br>
---
#### THESE FILES CAN BE DOWNLOADED BUT NEED TO BE IMPORTED

or just copy the string inside ( *items:[string]* ) to see what<br>
the item does anyways but there's a name inside of ( _items:[]_ ) you can read inside the string.

basically **Download** the .json files and import them into the saved items gui and yes there's a export and import button in<br>
the saved items.

- **Press N when hovering the item in a container** It opens the saved items.<br>
- **Saved Items** this contains all your saved items when you save it via the Giver.<br>
- **Export/Import** either export or import your `Saved Items.json` in here but the name doesn't matter but<br>
the file format does anyways.<br>
>[!NOTE]
> *There's more option when you hover in the items in<br>
> the saved items on a item so you can delete items more faster.<br>
> btw btw btw.*
