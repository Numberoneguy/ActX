# ActX 👌

### ALRIGHT I AM BACK.

#### I was gone for a while because I was debugging in a different environment. You might have seen a forked workspace in my profile [ eaglercraftx-1.8-workspace ].
#### It took me about like four-months to improve my GUI, NBT Handling ( NBT Tag Compound kinda annoyed me somehow :/ ), 
#### Since i can't write more stuff because i really don't remember things when i was debugging, tweaking the settings of my compilier ( gradlew ), a lot of stuff i can't seem to remember but anyways here are the list of the update i have added into `ActX v1.3.0.html` .
javascript doesn't exist yet.
## New Commands Introduced
`/block` This was originally supposed to block commands from other players. <br>
Since this command didn't work, i still kept it in the finished build.
<br><br>`/scoreboard players set/add/reset/remove "&7- &bPlaceholder" \<objective\> \<score\>` What's this supposed to do? i don't see any difference. well if you noticed the quotations `""` and `&6` . I added a new feature that allows spacing, colors, formats, e.t.c so you can able to add placeholders in your scoreboard ( that's displayed in the sidebar ofc ofc. ) 
<br><br>`/small` supposedly makes your player model small like a baby villager.
<br>
## Gamerules
`pvp` new gamerule so players won't get hit if disabled.
<br><br>`announceAdvancements` this is set to true, if disabled this prevents from announcing achievements to the chat.
<br><br>`doFallDamage` this is set to true, if disabled this prevents from gaining damage from fall damage.
<br><br>`allowHunger` this is set to true, if disbled this prevents from losing hunger.
<br><br>`doTntExplodes` gamerule inspired by erqixa (github user).
<br><br>`doImmediateRespawn` this has been in some of my clients but the name of the gamerule just already says it.
<br><br>`enableWorldEdit` s
## More stuff
I'm tired of writing stuff but here's some things that are added into the client.
- Chinese support (Simplified)
- Replaced the Shaders settings to ACT Settings.
- Client-side Command `/act help` for info. (expiremental)
- Website deployed (uses the old version of ACT) `yes-repo.vercel.app` (also is no available now
- Improved GUI
- More features when the Item Editor detects a specific item (potion, leather armor, spawn eggs `[more entities!]`)
- Improved previewing items.
- stuff i forgot to add.
